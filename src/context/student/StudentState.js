import React, { useReducer } from 'react';
import StudentContext from "./StudentContext";

const student = {
  firstName: "",
  secondName: "",
  phone: ""
}

const StudentState = (props) => {
  const initialState = {
    students: []
  }
}